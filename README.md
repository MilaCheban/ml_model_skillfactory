### README

---

#### Формулировка ML-задачи

Датасет содержит данные о диагностике рака молочной железы (WDBC — Wisconsin Diagnostic Breast Cancer). Каждая строка представляет один случай диагностики, где первый столбец — идентификатор пациента, второй — метка класса (`M` — злокачественная опухоль, `B` — доброкачественная), а остальные 30 столбцов — числовые признаки, вычисленные по изображению ядра клеток.

**ML-задача:**  
Решается задача **бинарной классификации**: предсказать, является ли опухоль злокачественной (`M`) или доброкачественной (`B`) на основе 30 числовых характеристик, полученных из изображения тканей.

---

#### Схематичное представление пайплайна

```
+-------------------+
| Загрузка данных   |
+-------------------+
          ↓
+-------------------+
| Предобработка     |
| данных            |
+-------------------+
          ↓
+-------------------+
| Разделение на     |
| обучающую и       |
| тестовую выборки  |
+-------------------+
          ↓
+-------------------+
| Масштабирование   |
| признаков         |
+-------------------+
          ↓
+-------------------+
| Выбор модели      |
| машинного обучения|
+-------------------+
          ↓
+-------------------+
| Обучение модели   |
+-------------------+
          ↓
+-------------------+
| Оценка качества   |
| модели (метрики)  |
+-------------------+
```

---

#### Краткое описание шагов

1. **Загрузка данных**:
   - Чтение данных из файла `wdbc.data.txt`.
   - Парсинг: разделение строк на идентификатор, метку класса и вектор признаков.
   - Преобразование меток класса (`M`, `B`) в числа (например, `1` и `0`).

2. **Предобработка данных**:
   - Проверка на пропущенные значения.
   - Анализ корреляций между признаками (при необходимости удаление сильно скоррелированных).
   - Визуализация распределений признаков для понимания данных.

3. **Разделение на обучающую и тестовую выборки**:
   - Данные делятся на обучающий и тестовый наборы (например, 80% / 20%).
   - Используется стратифицированное разделение для сохранения баланса классов.

4. **Масштабирование признаков**:
   - Применяется стандартизация (StandardScaler) или нормализация (MinMaxScaler) для улучшения сходимости моделей, чувствительных к масштабу признаков.

5. **Выбор модели**:
   - Рассматриваются несколько моделей: логистическая регрессия, SVM, деревья решений, случайный лес, градиентный бустинг.
   - Можно использовать GridSearchCV или RandomizedSearchCV для подбора гиперпараметров.

6. **Обучение модели**:
   - Обучение выбранной модели на обучающей выборке.
   - Сохранение обученной модели (по желанию).

7. **Оценка качества модели**:
   - Вычисление метрик: точность (accuracy), полнота (recall), точность положительного класса (precision), F1-мера, ROC AUC.
   - Построение матрицы ошибок (confusion matrix).
   - Анализ важности признаков (для некоторых моделей, например, случайный лес).

## 📁 ETL-компоненты (папка `etl/`)

Каждый из следующих скриптов реализует один этап ML-пайплайна. Все они модульные, принимают аргументы командной строки и могут запускаться как автономно, так и через Airflow.

### load_data.py  
Скрипт загружает данные из файла `wdbc.data.txt`, который содержит информацию о диагностике рака молочной железы. Данные преобразуются в формат DataFrame библиотеки pandas для дальнейшей обработки.

### preprocess.py  
Отвечает за предварительную очистку данных: удаляет ненужные столбцы, масштабирует признаки с помощью StandardScaler и делит выборку на обучающую и тестовую части. Также производится кодирование целевой переменной (`M` → 1, `B` → 0).

### train_model.py  
Выполняет обучение модели логистической регрессии на обучающей выборке. После обучения модель сохраняется в файл в формате `.pkl`.

### evaluate.py  
Рассчитывает метрики качества классификации (Accuracy, Precision, Recall, F1-score) на тестовой выборке и выводит результаты в консоль или сохраняет их во временном виде.

### save_results.py  
Финальный шаг пайплайна: сохраняет обученную модель и вычисленные метрики в указанную директорию. Модель записывается в файл `.pkl`, а метрики — в формате JSON.

---

## 💻 Инструкция по локальному запуску

1. Склонируйте репозиторий

2. Перейдите в корневую папку проекта и откройте терминал.

3. Установите и активируйте виртуальное окружение:
   ```bash
   conda env create -f project.yml
   conda activate project
   ```

4. Последовательно выполните скрипты:
   ```bash
   python etl/load_data.py
   python etl/preprocess.py
   python etl/train_model.py
   python etl/evaluate.py
   python etl/save_results.py
   ```

5. В результате в папке `results/` будут созданы:
   - `model.pkl` — обученная модель.
   - `metrics.json` — значения метрик.

📌 Пути к файлам можно указать вручную через аргументы командной строки при запуске каждого скрипта.

---

## 🚀 Инструкция по запуску через Apache Airflow (MacOS)

1. Клонируйте проект и перейдите в его корневую директорию.

2. Создайте и активируйте окружение:
   ```bash
   conda env create -f project.yml
   conda activate project
   ```

3. Установите переменную окружения `AIRFLOW_HOME`:
   ```bash
   export AIRFLOW_HOME=~/airflow
   ```

4. Инициализируйте базу данных Airflow:
   ```bash
   airflow db init
   ```

5. Создайте пользователя:
   ```bash
   airflow users create \
     --username admin \
     --firstname Admin \
     --lastname User \
     --role Admin \
     --email admin@example.com \
     --password admin
   ```

6. Отредактируйте файл `~/airflow/airflow.cfg`, указав абсолютный путь к папке с логами:
   ```
   base_log_folder = /absolute/path/to/logs
   ```

7. Переместите файл `pipeline_dag.py` в папку `dags` внутри `AIRFLOW_HOME`.

8. В файле DAG замените `<ABSOLUTE_PATH>` на реальный путь к вашему проекту.

9. Запустите планировщик Airflow:
   ```bash
   airflow scheduler
   ```

10. В другом терминале запустите веб-сервер:
    ```bash
    airflow webserver --port 8080
    ```

11. Откройте браузер и перейдите по адресу: http://localhost:8080. Авторизуйтесь под учетной записью администратора.

12. Найдите DAG с названием `breast_cancer_pipeline` и запустите его вручную.

13. Проверьте, что в папке `results/` созданы файлы модели и метрик.

---

## 📦 Хранение результатов работы

### Расположение:
Все выходные файлы сохраняются в папке `results/`.

### Артефакты:
- `model.pkl` — сериализованная модель машинного обучения.
- `metrics.json` — словарь с метриками качества модели.

### Пример содержимого `metrics.json`:
```json
{
  "accuracy": 0.95,
  "precision": 0.93,
  "recall": 0.94,
  "f1": 0.935
}

### Пример содержимого `metrics.json`:
Возможные проблемы:
- Проблемы с данными: пропущенные значения, некорректные типы данных.  
  Решение: добавить проверки валидации в `preprocess.py`.

- Ошибка загрузки датасета: если попытаться использовать удаленный источник данных.  
  Решение: используется локальная копия датасета, что повышает надежность.

- Низкая точность модели: несмотря на хорошие показатели, есть потенциал для улучшения.  
  Решение: внедрить подбор гиперпараметров через `GridSearchCV` или `Optuna`.

- Ограничения дискового пространства: при частом запуске пайплайна может накапливаться много версий моделей.  
  Решение: автоматическая очистка старых файлов или хранение только лучших версий.

Предложения по улучшению:
- Добавить поиск оптимальных гиперпараметров для модели.
- Реализовать механизм версионирования моделей.
- Организовать хранение результатов в облаке (например, AWS S3).
- Подключить систему уведомлений о сбоях и успешных запусках пайплайна.
- Настроить CI/CD для автоматического тестирования и деплоя.
